\CQUsection{实验与结果分析}

\CQUsubsection{实验设计概述}
本文实验以动脉瘤分割和破裂风险预测两个方面为研究对象，从体素级病灶识别能力、病例级风险判别能力两个方面来对所提出的方法进行系统的评价。分割实验用CTA三维体数据作为输入，考查模型输出的概率图和人工标注的掩膜之间的一致性；风险预测则使用临床信息与分割衍生特征作为输入，输出风险概率，考查模型对动脉瘤破裂状态的区分能力。

本文在数据集处理上严格遵守了训练集、验证集、测试集三者互不干扰的基本原则。训练集用来做模型参数的学习，验证集用来做超参数调节、阈值确定和模型选择，测试集只用来做最终性能的评价，以最小化信息泄漏造成的偏差。本文主要从分割任务的角度出发，对模型在训练后期的收敛稳定性、概率判别能力以及病灶检出能力进行了研究。对于风险预测任务，本文主要对比了深度交叉注意力模型和传统的机器学习模型在多项评价指标上的表现，并且对不同模型的判别特点和适用场景进行了分析。

\CQUsubsection{实验环境及基本设置}



实验是在通用Linux计算环境下完成的，具体环境见表~\ref{tab:env_setup}。深度学习模型使用主流张量计算框架进行训练和推理，医学影像读取、空间重采样、数值计算和统计分析都依靠成熟的科学计算工具来完成。为了保证实验过程的可重复性，所有的训练与验证流程均是在统一的数据集，处理，训练，验证流程下进行记录的。

\begin{table}[!htbp]
\centering
\caption{系统环境配置}
\label{tab:env_setup}
\small
\renewcommand{\arraystretch}{0.88}
\begin{tabular}{ll}
\toprule
项目 & 配置 \\
\midrule
操作系统内核 & Linux 5.15.0-25-generic \\
GPU & NVIDIA GeForce RTX 5090 \\
显存容量 & 32607 MiB \\
NVIDIA驱动版本 & 580.95.05 \\
CUDA版本 & 13.0 \\
Python版本 & 3.14.5 \\
运行环境 & uv \\
\bottomrule
\end{tabular}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

在本实验的过程中，分割模型的输入是三维CTA体数据。训练阶段使用局部patch学习方法来解决部分情况下的显存限制问题，同时提高小目标病灶在训练过程中可见性来提升模型对小型病灶的敏感程度；验证阶段使用滑窗方式进行整例推理，保证输出结果有空间完整性。风险预测任务输入是病例级表格特征，数值变量做标准化处理，类别变量做离散嵌入表示，模型输出为病例级破裂概率。考虑到风险预测任务中正负样本分布一般存在不平衡情况，本文在训练阶段使用类别加权策略，同时在验证集上搜索最优判别阈值来提升模型对于少数类样本的识别能力，并给出模型适用的外部参数。

保证比较的公平性，所有的对比模型都采用相同的划分方式，测试集不参与训练、调参和阈值选择。分割实验中模型选择主要根据验证集的重叠指标和阳性检出能力来决定；风险预测实验中，阈值选择只根据验证集输出概率完成，之后将该阈值固定在测试集上报告性能。该设计可以减小由于测试集反馈所造成的乐观偏差，使实验结果更真实地反映出模型对于未见样本的泛化表现。

分割实验的数据划分与训练设置如表~\ref{tab:seg_data_training_setup}和表~\ref{tab:seg_hyperparams}所示。本实验共使用646例三维CTA数据，其中536例用于模型训练，110例用于验证和测试，训练集与验证/测试集比例约为83\%:17\%。数据集为自建数据集，主要分为两类，一类为完全未处理的CTA与Mask label，一类为取上半部分也就是以大脑部分为中心的切割后版本。二者均用于训练，权重一致。

\begin{table}[!htbp]
\centering
\caption{分割实验数据划分}
\label{tab:seg_data_training_setup}
\small
\renewcommand{\arraystretch}{0.88}
\begin{tabular}{lcl}
\toprule
数据部分 & 病例数 & 数据来源 \\
\midrule
训练集 & 536 & singlecrop1、singlecrop2、singlecrop3、MultiCrop \\
验证/测试集 & 110 & Multi、singlecrop4 \\
总计 & 646 & 全部分割实验数据 \\
\bottomrule
\end{tabular}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

\begin{table}[!htbp]
\centering
\caption{分割模型训练超参数设置}
\label{tab:seg_hyperparams}
\small
\renewcommand{\arraystretch}{0.88}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{ll}
\toprule
项目 & 设置 \\
\midrule
输入/输出通道数 & 1/1 \\
基础通道数 & 48 \\
网络深度 & 5 \\
归一化方式 & Instance Normalization \\
激活函数 & Leaky ReLU \\
Dropout & 0.1 \\
训练轮数 & 20 epochs \\
Batch size & 1 \\
优化器 & AdamW \\
初始学习率 & $5\times10^{-4}$ \\
权重衰减 & $1\times10^{-5}$ \\
学习率策略 & 前5个epoch保持初始学习率，随后使用余弦退火 \\
最小学习率 & $1\times10^{-6}$ \\
损失函数 & BCE与Tversky损失的组合 \\
Tversky参数 & $\alpha=0.3,\ \beta=0.7$ \\
训练补丁大小 & $64\times64\times64$ \\
推理有效窗口 & $48\times48\times48$ \\
每例采样补丁数 & 64 \\
补丁采样策略 & 前景优先采样，背景/前景比例为2:1 \\
随机种子 & 42 \\
计算设备 & CUDA \\
\bottomrule
\end{tabular}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

当前实验配置中数据处理主要包括体数据重采样、强度归一化、局部补丁裁剪以及前景优先采样。对于动脉瘤这类小目标分割任务，前景优先采样可以提高病灶体素在训练批次中的出现频率，从而缓解严重类别不平衡造成的背景偏置以加速训练速度与模型对病灶的检测能力。

\CQUsubsection{评价指标}
\CQUsubsubsection{分割任务评价指标}
对于分割任务评价，我们从概率判别能力、区域重叠程度和阳性体素检出能力三方面来考虑。设真实前景集合为 $Y$，预测前景集合为 $\hat{Y}$，Dice 系数定义为
\begin{equation}
\mathrm{Dice}=\frac{2|Y\cap \hat{Y}|}{|Y|+|\hat{Y}|}.
\label{eq:eval-dice}
\end{equation}
Dice 系数越大，说明预测区域和真实区域的重叠程度越大。由于动脉瘤一般体积小，边界的微小偏差就会造成 Dice 明显的波动，所以该指标对于分割质量比较敏感。

从体素级二分类的角度出发，真阳性、假阳性、真阴性和假阴性分别记作TP、FP、TN和FN。召回率定义为
\begin{equation}
\mathrm{Recall}=\frac{\mathrm{TP}}{\mathrm{TP}+\mathrm{FN}},
\label{eq:eval-recall}
\end{equation}
用来衡量真实前景体素被正确识别出来的比例。对于医学筛查场景来说，召回率具有重要的意义，因为漏检病灶的代价往往大于将正常人误判为病人的代价。精确率则定义为
\begin{equation}
\mathrm{Precision}=\frac{\mathrm{TP}}{\mathrm{TP}+\mathrm{FP}},
\label{eq:eval-precision}
\end{equation}
用来衡量预测为前景的体素中真实属于前景的比例。在这二者的基础上，另外，F1 score 可以表示为
\begin{equation}
F_1=\frac{2\times \mathrm{Precision}\times \mathrm{Recall}}{\mathrm{Precision}+\mathrm{Recall}}.
\label{eq:eval-f1}
\end{equation}
相比前两者，此更能表现模型对病灶判别的综合能力。
AUC 用阈值无关的方式描述模型对前景和背景体素的整体排序能力，可以用来刻画概率输出的可分性。如果用真阳性率TPR(τ)和假阳性率FPR(τ)分别表示阈值τ下敏感度和误报率，那么ROC-AUC就可以形式化地表示为
\begin{equation}
\mathrm{AUC}=\int_{0}^{1}\mathrm{TPR}\left(\mathrm{FPR}^{-1}(u)\right)du,
\label{eq:eval-auc}
\end{equation}
此式用于解释在所有的可能的阈值下，综合评价模型把前景体素排序在背景体素之前的能力。

考虑到小目标分割中边界少量偏差会造成较大指标波动，本文主要以 Dice、Recall、F1 与 AUC 为核心指标，并结合可视化结果讨论边界误差。

\CQUsubsubsection{风险预测评价指标}
风险预测属于二分类问题，仅仅使用用Accuracy来评价模型并不足以完整展现模型的性能。其定义为
\begin{equation}
\mathrm{Accuracy}=\frac{\mathrm{TP}+\mathrm{TN}}{\mathrm{TP}+\mathrm{TN}+\mathrm{FP}+\mathrm{FN}},
\label{eq:accuracy}
\end{equation}
准确率虽然反映整体分类正确率，但是仍然无法全面评估模型性能。因此本文同时给出Precision、Recall、F1和AUC等指标来全面反映模型在风险识别任务上的表现。

对于风险预测模型输出的概率 $\hat{p}_i$，其二分类结果由阈值 $\tau$ 决定：
\begin{equation}
\hat{y}_i=
\begin{cases}
1, & \hat{p}_i\geq \tau,\\
0, & \hat{p}_i<\tau.
\end{cases}
\label{eq:risk-threshold}
\end{equation}
本文在验证集上搜索较优阈值，并将其固定用于测试集评估。该策略可以利用数据分布以及任务目标自动调整决策边界，从而降低默认阈值对于实验结果的影响。在本文中，我们以F1值作为模型评价的优先选择准则。

除此之外，医学风险预测还要考虑概率输出的可解释性和校准性。理想情况下，当模型对一组病例给出接近 $p$ 的风险概率时，该组病例中实际阳性比例也应该接近 $p$。将样本按照预测概率分成 $K$ 个区间 $B_k$，则期望校准误差可以表示为
\begin{equation}
\mathrm{ECE}=\sum_{k=1}^{K}\frac{|B_k|}{N}\left|\operatorname{acc}(B_k)-\operatorname{conf}(B_k)\right|,
\label{eq:ece}
\end{equation}
其中 $\operatorname{acc}(B_k)$ 是第 $k$ 个区间内的真实阳性比例，$\operatorname{conf}(B_k)$ 是平均预测概率。虽然本文主要对分类判别指标进行报告，但是校准分析对于后续的临床使用有着非常重要的意义，不仅是因为风险概率会改变二分类决策，它还会影响临床复核、随访优先级等临床处理的安排。

为了方便说明各类评价指标在本文中所起的作用，对本章主要指标以及它们的分析侧重点进行了归纳。

\begin{table}[!htbp]
\centering
\caption{主要评价指标及其分析侧重点}
\label{tab:metric_summary}
\small
\renewcommand{\arraystretch}{0.88}
\begin{tabular}{lll}
\toprule
任务类型 & 指标 & 主要反映内容 \\
\midrule
分割任务 & Dice & 预测区域与真实区域的重叠程度 \\
分割任务 & Recall & 病灶前景体素的检出能力 \\
分割任务 & Precision & 前景预测结果的可靠性 \\
分割任务 & AUC & 前景与背景体素的整体可分性 \\
风险预测任务 & Accuracy & 整体分类正确率 \\
风险预测任务 & Precision & 高风险预测结果的准确性 \\
风险预测任务 & Recall & 破裂病例的识别能力 \\
风险预测任务 & F1 & Precision 与 Recall 的综合平衡 \\
风险预测任务 & AUC & 破裂与未破裂病例的排序能力 \\
\bottomrule
\end{tabular}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

\CQUsubsection{动脉瘤分割实验}
\CQUsubsubsection{实验目的与训练过程}

分割实验的主要目的是训练出合适的三维 Attention U-Net 模型及其改进后的 Attention U-Net模型，同时根据它们对颅内动脉瘤的定位能力以及轮廓恢复能力来找到最优模型并给出评价。训练时，模型用局部补丁样本学习病灶及其邻域的空间结构，直到在验证和测试阶段才会对整例 CTA 体数据进行滑窗推理。由于动脉瘤在空间上非常稀疏，实验不但重视病灶前景的召回率，还重视在复杂的血管背景下的误检控制。从训练过程来看，在训练网络的后期，每个网络已经基本完成从低层纹理到高层病灶语义的表征学习。本文主要统计了该阶段的验证结果，以此来观察不同模型的实际性能如何。此时，AUC 和 Recall 均处于相对稳定且较高的区间，说明模型对于前景和背景的概率排序能力较强，而且病灶检出性能没有出现明显的波动。


\CQUsubsubsection{分割结果和阈值分析}


需要指出的是，较高的AUC并不能说明分割结果在空间上已经达到了完全理想的状态。AUC主要用来评价不同的阈值下模型对于正负体素整体排序的质量，但是最终二值掩膜的空间一致性还会受到阈值选择、病灶体积、边界清晰度和重建方式等影响。对于小目标病灶来说，虽然模型在概率排序上表现良好，但是不恰当的阈值仍然会造成边界收缩、局部断裂或者离散误检区域。因此对分割模型进行评价的时候应该把Dice、Recall以及典型病例的可视化结果结合起来分析，而不是仅仅依靠一个指标来判断。

从阈值扫描结果可知，分割阈值 $\tau_s$ 越大，模型预测越保守，误检体素数目会减少，但是Recall会同步下降；反之，当阈值减小时，模型会倾向于保留低置信度的疑似前景，Recall会增加，但是误检风险也会增加。其决策形式可以表示为
\begin{equation}
\hat{M}_{i,j,k}(\tau_s)=\mathbb{I}\left(P_{i,j,k}\geq \tau_s\right).
\label{eq:threshold-decision}
\end{equation}

其中，$\hat{M}_{i,j,k}(\tau_s)$ 表示模型对第 $i$ 行第 $j$ 列第 $k$ 个体素的预测结果，$P_{i,j,k}$ 表示模型对第 $i$ 行第 $j$ 列第 $k$ 个体素的概率。当 $\tau_s$ 增大时，满足条件的前景集合会越来越小，漏检概率也随之增大。对于医学辅助分析场景来说，保持较高的病灶检出能力很重要，但是同时也要控制误检区域的大小，保证后续结构定量分析以及临床展示的可解释性。

\CQUsubsubsection{可视化分析与误差来源}
从典型的病例可视化结果可知，模型对于大部分病灶区域能够较好地覆盖动脉瘤主体，在局部边界上形成比较连续的响应。对于形态比较规则、对比度比较高的病灶来说，预测掩膜和人工标注的一致性较高；但是当病灶体积小、边缘对比度低、与载瘤血管过渡模糊或者邻近高密度结构干扰明显的时候，模型就会出现局部漏检、边界偏移或者少量误检。

为了更直观地比较不同结构在同一病例上的预测差异，本文选取同一CTA病例的同一空间位置，对四种分割模型的预测结果和人工标注进行可视化展示，如图~\ref{fig:seg_visual_compare}所示。四个预测图分别对应基础3D U-Net、3D U-Net + ASPP、3D U-Net + CoordAttention以及3D U-Net + ASPP + CoordAttention，最后一幅为人工标注结果。可以看出，不同模型在病灶响应范围和误检区域上存在差异：部分模型预测结果更加保守，比如带有ASPP的的模型在预测上更加保守，同时其对于其他位置的多余检测也较少，虽然在一定程度上实现了更精准的模型预测，但也可能造成病灶主体覆盖不足，在综合程度上，其有着更为精准的预测结果；部分增强模型如带有坐标注意力的两个模型在疑似病灶区域形成更明显的响应，但同时也有着多余的零星的多余展示结果。该可视化结果说明，分割模型的定量指标需要结合空间显示结果共同解释，尤其是在小目标和强背景干扰的CTA场景中，仅依靠单一指标难以完整反映模型行为，因此我们认为，应该在模型的预测结果上，辅以合理的临床辅助分析来完善医生的整体决策。

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_u.png}
        \small (a) 3D U-Net
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_ua.png}
        \small (b) U-Net + ASPP
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_uc.png}
        \small (c) U-Net + CoordAttention
    \end{minipage}
    \vspace{0.5em}
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_uac.png}
        \small (d) U-Net + ASPP + CoordAttention
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/label.png}
        \small (e) Label
    \end{minipage}
    \caption{同一病例下不同分割模型预测结果与人工标注的可视化比较}
    \label{fig:seg_visual_compare}
\end{figure}

为了进一步观察病灶局部区域的预测差异，本文对图~\ref{fig:seg_visual_compare}中的病灶邻域进行局部放大，并给出不同模型在放大区域上的对比结果，如图~\ref{fig:seg_visual_compare_zoom}所示。局部放大图可以更清楚地显示模型在小病灶边界、局部连通性以及离散误检点上的差异。基础模型和坐标注意力模型在局部区域中对疑似前景有较强响应，但也可能保留额外的零散响应；ASPP相关模型在放大区域内表现得相对集中，有助于控制非目标区域的预测响应。与人工标注对照可以看出，局部放大结果能够补充全局切片可视化中不容易观察到的边界误差和小范围误检，因此更适合用于分析小目标分割模型的实际预测行为。

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_u_zoom_in.png}
        \small (a) 3D U-Net
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_ua_zoom_in.png}
        \small (b) U-Net + ASPP
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_uc_zoom_in.png}
        \small (c) U-Net + CoordAttention
    \end{minipage}
    \vspace{0.5em}
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/res_uac_zoom_in.png}
        \small (d) U-Net + ASPP + CoordAttention
    \end{minipage}
    \hfill
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figure/label_zoom_in.png}
        \small (e) Label
    \end{minipage}
    \caption{同一病例病灶局部放大区域下不同分割模型预测结果与人工标注的可视化比较}
    \label{fig:seg_visual_compare_zoom}
\end{figure}

这类误差具有一定的普遍性。首先，在数据本身层面，小病灶在多次下采样过程中容易造成空间信息的丢失，即使使用跳跃连接进行细节的补充，也未必完全恢复极细小结构边界；另一方面，CTA中血管增强区域和动脉瘤前景在灰度分布上存在相似性，模型需要依靠空间上下文和拓扑关系而不是仅仅依靠强度差异来做出判断。因此，如果后续要提高分割效果，可以就更具针对性的前景采样、边界敏感损失设计、多尺度监督以及稳健的后处理策略等角度进行优化。


\CQUsubsubsection{分割模型消融分析}
为了分析模型性能提升的来源，本文从结构设计角度对分割模型进行了消融讨论。可以把基础三维 Attention U-Net、引入 ASPP 的增强模型、进一步加入三维坐标注意力的模型作为逐步增强的结构序列。基础模型主要是依靠编码器—解码器结构和注意力门控来完成病灶的恢复；ASPP模块用多尺度空洞卷积扩大感受野，有利于增强模型对不同尺度病灶及其邻域上下文的刻画能力；三维坐标注意力又加入了方向感知的位置建模，对边界模糊、形态不规则或者空间走向复杂的病灶有潜在的优势。

从现有的实验结果可以看出，不同增强模块对分割性能的影响存在差异。为了方便比较，表~\ref{tab:seg_ablation}对四种模型在验证/测试集上的分割消融结果进行了汇总。表中参数量表示模型可训练参数规模，Dice为验证过程中记录的整例顺序滑窗评价结果，AUC、Precision、Recall、F1和IoU均为阈值0.5下的体素级统计结果；Best F1表示在候选阈值范围内搜索得到的最优F1值。由于动脉瘤前景体素占比极低，Dice、F1和Recall比整体准确率更能反映模型对病灶区域的实际识别能力。

\begin{table}[!htbp]
\centering
\caption{分割模型消融结果比较}
\label{tab:seg_ablation}
\small
\renewcommand{\arraystretch}{0.88}
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccccc}
\toprule
模型 & 参数量/M & Dice & AUC & Precision & Recall & F1 & IoU & Best F1 \\
\midrule
3D U-Net baseline & 51.19 & 0.7320 & 0.9367 & 0.3484 & 0.7995 & 0.4853 & 0.3204 & 0.5401 \\
3D U-Net + ASPP & 67.71 & \textbf{0.9263} & 0.8402 & \textbf{0.5381} & 0.6398 & \textbf{0.5846} & \textbf{0.4130} & \textbf{0.5902} \\
3D U-Net + CoordAttention & 51.34 & 0.6641 & \textbf{0.9295} & 0.2302 & \textbf{0.7801} & 0.3555 & 0.2161 & 0.4116 \\
3D U-Net + ASPP + CoordAttention & 67.86 & 0.6652 & 0.9216 & 0.2880 & 0.7539 & 0.4168 & 0.2633 & 0.5147 \\
\bottomrule
\end{tabular}%
}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

从表~\ref{tab:seg_ablation}可以看出，单独加入ASPP的模型取得了最高的Dice、Precision、F1、IoU和Best F1，说明多尺度空洞卷积能够明显改善当前数据上的病灶区域成形质量和阳性预测可靠性。基础U-Net虽然Dice较低，但在阈值0.5下具有较高Recall和较高Best F1，说明其概率输出对前景体素有一定敏感性，但空间重叠质量仍然不足。单独加入CoordAttention后，AUC保持在较高水平，说明方向敏感注意力对体素概率排序有帮助，但在固定阈值下Precision和F1下降，我们认为其存在更多低置信度前景响应。ASPP与CoordAttention联合使用后，Recall和AUC保持较高，但F1低于单独ASPP模型，表明两个模块的联合效果并未在当前训练设置下完全转化为更优的二值分割结果。

由于动脉瘤分割属于一种高度不平衡的体素级任务，背景体素占的比例远大于病灶体素，所以整体像素一致性会受到很多真阴性体素的主导，其解释力比较小。相比之下，Dice、Recall、F1等更能反映病灶区域识别能力的指标，可以更真实地反映模型在本任务上的性能差异。因此本文在消融分析时更重视结构对病灶区域识别质量的改善，而不是单纯地关注整体一致性指标。

\CQUsubsection{破裂风险预测实验}
\CQUsubsubsection{实验目的与模型设置}
破裂风险预测实验用以评价多源表格特征对于病例级破裂状态的判别能力，同时比较交叉注意力模型和传统机器学习模型在中小规模医学数据条件下的表现。所有模型都使用相同的训练集、验证集、测试集划分来评估，验证集上完成阈值选择，保证各个模型之间有共同的比较基础。

风险预测数据来自病例级表格文件，标签列为“破裂”。在特征构造阶段，本文首先读取临床变量和影像组学候选变量，并剔除诊断信息类字段；随后将临床字段与影像组学交集字段合并作为候选输入。根据特征列和原始表格的匹配结果，风险数据处理中共记录影像组学候选特征1409个，风险表格字段共2182个，可用于建模的交集特征1379个。再加上9个临床字段后，模型的候选输入字段约为1388个。数据清洗时会删除全空列和重复样本，并把字段名中的多余空白去除。对于每个候选字段，若其转换为数值后的非缺失比例不低于80\%，则作为数值型变量处理；否则作为类别型变量处理。数值型变量使用中位数填补缺失值，并做均值方差标准化；类别型变量转换为字符串类别后进行整数编码，并在交叉注意力模型中通过嵌入层映射到隐空间。

风险预测任务的数据划分和处理流程如表~\ref{tab:risk_data_process}所示。数据集先按照8:2划分训练验证部分和测试集，再从训练验证部分中划分出验证集，最终比例约为70\%:10\%:20\%。划分时在正负样本均存在的情况下采用分层抽样，以尽量保持训练集、验证集和测试集中的类别比例一致。训练过程中使用验证集进行阈值选择和模型选择，测试集仅用于最终性能报告。

\begin{table}[!htbp]
\centering
\caption{风险预测数据处理与划分设置}
\label{tab:risk_data_process}
\small
\renewcommand{\arraystretch}{0.88}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{ll}
\toprule
项目 & 设置 \\
\midrule
预测任务 & 动脉瘤破裂风险二分类 \\
标签字段 & 破裂 \\
临床字段数 & 9 \\
临床字段 & 性别、年龄、高血压、心脏病、糖尿病等 \\
影像组学候选特征数 & 1409 \\
风险表格字段数 & 2182 \\
交集特征数 & 1379 \\
候选输入字段数 & 约1388 \\
数值/类别判定 & 数值转换非缺失比例不低于80\%时作为数值变量 \\
数值缺失值处理 & 中位数填补 \\
数值标准化 & 均值方差标准化 \\
类别变量处理 & 字符串类别映射为整数编码，并使用嵌入层表示 \\
数据划分 & 训练集约70\%，验证集约10\%，测试集约20\% \\
划分方式 & 固定随机种子42，优先使用分层抽样 \\
\bottomrule
\end{tabular}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

本文所包含的比较模型有交叉注意力深度表格模型、随机森林、逻辑回归、支持向量机、$k$近邻以及融合模型。逻辑回归可以看作是线性可解释的基线，随机森林是集成树模型，支持向量机和 $k$近邻分别是基于核映射和局部相似性的传统分类方法，而交叉注意力模型是利用字段交互学习高阶依赖关系的深度方法。融合模型把树模型和注意力模型的输出结合起来，用来检验不同模型范式之间的互补性。

交叉注意力风险预测模型的训练设置如表~\ref{tab:risk_train_setup}所示。模型将数值字段和类别字段分别构造成字段级token，其中数值字段经过可学习线性投影和字段偏置形成数值token，类别字段经过嵌入层形成类别token。随后模型用双向交叉注意力模块在数值token和类别token之间进行信息交互，并将两类token池化后的结果拼接后输入输出层，得到病例级破裂风险概率。损失函数采用带正类权重的二元交叉熵损失，以缓解破裂和未破裂样本分布不均衡造成的训练偏置。

\begin{table}[!htbp]
\centering
\caption{风险预测模型训练参数设置}
\label{tab:risk_train_setup}
\small
\renewcommand{\arraystretch}{0.88}
\begin{tabular}{ll}
\toprule
项目 & 设置 \\
\midrule
模型 & Risk Cross-Attention \\
隐藏维度 & 32 \\
注意力头数 & 4 \\
交叉注意力层数 & 1 \\
Dropout & 0.35 \\
训练轮数上限 & 500 epochs \\
Batch size & 64 \\
优化器 & AdamW \\
学习率 & $2\times10^{-4}$ \\
权重衰减 & $5\times10^{-4}$ \\
损失函数 & 带正类权重的BCEWithLogitsLoss \\
正类权重 & 由训练集中负样本数/正样本数自动计算 \\
早停耐心轮数 & 60 \\
早停最小改进 & $1\times10^{-4}$ \\
阈值搜索范围 & 0.05到0.95 \\
阈值搜索步数 & 37 \\
模型选择指标 & 验证集F1值 \\
测试策略 & 固定验证集最优阈值后在测试集报告结果 \\
\bottomrule
\end{tabular}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

传统机器学习基线也使用相同的数据划分和候选特征集合。对于传统模型，数值特征使用中位数填补，类别特征使用众数填补并进行one-hot编码。逻辑回归采用类别平衡权重和liblinear求解器，随机森林使用300棵树并设置类别平衡权重，SVM使用RBF核并开启概率输出，KNN采用距离加权策略。所有模型均在验证集上搜索使F1值最优的分类阈值，并将该阈值固定后用于测试集评价。

\CQUsubsubsection{对比实验结果}
风险预测模型测试集结果见表~\ref{tab:risk_result}。表中 Threshold 表示由验证集确定并固定用于测试集评估的分类阈值。

\begin{table}[!htbp]
\centering
\caption{风险预测模型测试集对比结果}
\label{tab:risk_result}
\small
\renewcommand{\arraystretch}{0.88}
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
模型 & Accuracy & Precision & Recall & F1 & AUC & Threshold \\
\midrule
Forest-Attention Ensemble & 0.7944 & 0.6931 & 0.7778 & \textbf{0.7330} & \textbf{0.8568} & 0.350 \\
Risk Cross-Attention & 0.7852 & 0.6832 & 0.7667 & 0.7266 & 0.8546 & 0.325 \\
Random Forest & 0.7419 & 0.6182 & 0.7556 & 0.6800 & 0.8284 & 0.400 \\
Logistic Regression & 0.6895 & 0.5512 & 0.7778 & 0.6452 & 0.7342 & 0.450 \\
KNN & 0.5766 & 0.4576 & \textbf{0.9000} & 0.6067 & 0.6966 & 0.200 \\
SVM-RBF & 0.5565 & 0.4451 & \textbf{0.9000} & 0.5956 & 0.6732 & 0.300 \\
\bottomrule
\end{tabular}%
}
\normalsize
\renewcommand{\arraystretch}{1}
\end{table}

根据表~\ref{tab:risk_result}可知，Forest-Attention Ensemble在测试集上获得最高的F1值0.7330和AUC值0.8568，说明传统树模型和交叉注意力网络模型在当前任务中具有一定的互补优势。Risk Cross-Attention模型得到F1值为0.7266、AUC值为0.8546，其性能与最优的融合模型没有产生较为明显的差距，但却明显优于逻辑回归、支持向量机、k近邻等传统单模型基线。从此分析出，相比于传统的部分机器学习模型，基于字段级交互的深度建模方式可以很好地捕捉到临床变量和影像表型变量之间非线性关系。

从随机森林在Accuracy、F1和AUC等指标上都比很多传统基线好可以看出集成树模型对于中小规模表格数据的有更强的适应能力，以及拟合水平。相比之下，KNN和SVM-RBF模型虽然能在测试过程中使Recall达到0.9000，展现出了较强的阳性病例检出倾向，但是它们的Precision分别仅仅只有0.4576和0.4451，F1以及AUC也都是处在较低水平。因此我们并不将它认作为较好的结果，这两类更倾向于把样本判定为高风险类别的模型虽然可以提高召回能力，但是也明显增加了误报率，整体结果并没有超过随机森林或者随机森林-注意力融合模型。对于逻辑回归模型，其虽然具有很强的可解释性，但是它的AUC仅仅只有0.7342，可见线性决策边界很难很好地刻画出当前多源特征中复杂的非线性关系。

随机森林用多个决策树的集成形成分段非线性判别函数，可以较好的处理变量之间的条件分裂关系。交叉注意力模型用字段token之间注意力权重的学习来得到全局交互，其判别函数可以抽象为
\begin{equation}
\hat{p}=\sigma\left(F_{\omega}(e_1,e_2,\ldots,e_{m+q})\right),
\label{eq:cross-attn-risk}
\end{equation}
其中 $F_{\omega}(\cdot)$ 表示由多层注意力和非线性映射构成的可学习函数， $e_i$ 表示第 $i$ 个字段的表示向量，$m$ 表示临床特征数量，$q$ 表示影像特征数量。该类模型具有更强的表达能力，但是对样本规模、正则化和数据噪声更敏感。因此，实验结果中深度模型与集成树模型表现接近，符合中小规模医学表格数据的一般规律。同时又能在深度维度空间上补足随机森林模型难以捕获多维数据关联上的不足。

\CQUsubsubsection{阈值选择与不平衡分类讨论}

在我的实验中，风险预测实验中各个模型的最优阈值都小于或者接近于0.5，说明默认的判别阈值不能很好地适应医学风险识别任务的实际需要。对于输出概率 $\hat{p}$ 而言，降低阈值会使更多的病例被判定为高风险，从而提高 Recall，但是也会降低 Precision；提高阈值会产生相反的效果。因此，阈值选择本质上是在漏诊风险和误报代价之间做出取舍。

从临床辅助分析的角度来说，破裂风险预测一般会关注高风险病例的检出能力，因此Recall比其他的属性更有参考价值，但是当Precision较低的时候，就会出现过多的低风险病例被误判为高风险的情况，进而加重后续检查、复核和干预的负担。F1值是通过综合考虑Precision和Recall来给动脉瘤破裂风险预测的二分类任务提供一个更平衡的评价方式。AUC是用全阈值范围来反映模型对于阳性、阴性病例排序的能力，可以用来比较模型之间的总体判别能力。在实际应用时应该通过实际情况合理选择阈值。


\CQUsubsubsection{风险模型结果讨论}

从各个指标来看，基于交叉注意力的深度表格模型在本次任务中具有较强的竞争力，但是并没有全面超过所有的传统方法。主要是由于交叉注意力机制可以显式地捕捉到异构字段之间复杂的依赖关系，在处理多源医学表格数据的时候有较好的表达能力；另一方面，在样本规模适中的情况下，传统的集成学习模型一般会比其他的单一模型有较好的稳健性与适应性，同时带来较低的过拟合风险。

因此，深度模型和传统模型之间不是简单的替代关系，而是根据不同的数据条件和评价目标而形成了潜在的互补优势。融合模型在本实验中取得最优综合性能的结果证明了上述的判断。由此可知，使用树模型的分裂规则以及使用注意力机制的表示学习，实际上捕捉到了不同的判别模式，二者结合起来可以缓解单一模型的偏差来提高整体分类效果。也给后面探索更系统的模型融合策略提供支持，有利于进一步提高风险预测的稳定性以及泛化能力。


\CQUsubsection{结果综合讨论}

根据综合分割、风险预测实验结果可知，本文方法在目前数据集上已经形成了从影像结构识别到病例风险估计的完整分析流程。分割模型在体素级AUC和Recall上表现良好，由此我们可以看出它可以把病灶和复杂的血管背景较好地区分开来；其中，加入ASPP后的模型在Dice和F1上表现最好，表明多尺度上下文建模对当前动脉瘤分割任务具有较强作用。风险预测模型在F1和AUC上接近最优融合模型，说明交叉注意力机制在多源表格特征建模中具有较好的适用性。

从方法构成上讲，分割任务用注意力门控、ASPP、三维坐标注意力分别实现了背景抑制、多尺度上下文聚合、方向位置建模。实验结果表明，多尺度上下文信息对病灶区域成形质量的改善更加直接，而方向位置注意力的作用需要结合阈值校准和更充分训练进一步分析。在风险预测任务中，字段级嵌入和交叉注意力机制把传统的特征拼接转变成可以学习的交互表示，使模型在病例层面可以捕捉临床变量、影像表型、形态特征之间的潜在依赖关系。

同时也会出现一些值得注意的问题。医学影像数据规模小、标注成本高、单次数据划分结果容易受到样本分布的影响等；风险预测任务中的类别不平衡问题会严重影响到阈值的选择和指标的解读；深度表格模型在中小样本医学任务中并不总是稳定优于传统集成模型，合理的正则化、特征选择、概率校准以及外部验证仍然需要。

\CQUsubsection{系统展示}
为了保证整个方法流程的完整性，本文把分割推理、特征提取和风险预测组织成一个端到端的演示系统。该系统可以从输入CTA影像开始，依次完成病灶概率图生成、二值掩膜输出、形态特征提取以及破裂风险概率估计，最后以可视化的方式呈现关键结果。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\linewidth]{figure/display.png}
    \caption{系统展示界面}
    \label{fig:display_picture}
\end{figure}

如图~\ref{fig:display_picture}所示，系统界面主要包含以下几个功能模块：影像上传模块用于接收用户上传的CTA体数据，并展示在用户界面中，位于界面右上角部分；分割推理模块调用训练好的三维Attention U-Net模型对输入影像进行动脉瘤区域识别，在此过程中会流式分割CTA的三维体数据，之后进行单独patch预测并拼装后返回给用户界面展示；风险预测模块将形态特征与临床信息结合，通过交叉注意力模型输出病例级破裂风险概率。


\CQUsubsection{本章小结}
本章从实验设计、评价指标、分割实验、风险预测对比、消融分析以及结果讨论等几个方面对本文的方法进行了比较系统的实验性评价。结果表明，分割模型在当前的验证数据上具有较好的体素级判别能力以及病灶检出能力；风险预测模型可以有效地利用多源表格特征，在测试集上取得接近最优融合模型的综合表现。本文方法在病灶识别和风险评估两个方面基本达到了预期目标，但是稳健性、泛化性等的检测和提升还需要通过大规模、多中心的多轮重复实验来进一步验证和完善。